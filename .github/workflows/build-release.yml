name: Build Plugin ZIP

on:
  push:
    tags:
      - 'v*'
  workflow_dispatch:

permissions:
  contents: write

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Prepare plugin
        run: |
          mkdir -p build/lucky-wheel-pro
          rsync -av --exclude='.git' --exclude='.github' --exclude='build' ./ build/lucky-wheel-pro/

      - name: Create ZIP
        run: |
          cd build
          zip -r lucky-wheel-pro.zip lucky-wheel-pro

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: lucky-wheel-pro
          path: build/lucky-wheel-pro.zip

      - name: Publish GitHub Release
        if: startsWith(github.ref, 'refs/tags/v')
        uses: softprops/action-gh-release@v2
        with:
          files: build/lucky-wheel-pro.zip
          generate_release_notes: true
